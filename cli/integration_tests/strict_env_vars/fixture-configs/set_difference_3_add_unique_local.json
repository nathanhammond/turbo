{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "experimentalPassThroughEnv": ["DUPLICATED", "UNIQUE"]
    }
  },
  "experimentalGlobalPassThroughEnv": ["DUPLICATED"]
}
